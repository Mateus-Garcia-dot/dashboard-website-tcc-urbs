export class Shape {

    SHP: string;
    COD: string;
    COORD: number[];

    constructor(SHP: string, COD: string, COORD: number[]) {
        this.SHP = SHP;
        this.COD = COD;
        this.COORD = COORD;
    }
    
}